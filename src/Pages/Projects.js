import React from 'react';
import Navbar from '../Shareable/Navbar';
import Footer from '../Shareable/Footer';

const Projects = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h1>All projects will shown here</h1>
            <Footer></Footer>
        </div>
    );
};

export default Projects;