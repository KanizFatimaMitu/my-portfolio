import React from 'react';
import Navbar from '../Shareable/Navbar';
import Footer from '../Shareable/Footer';

const About = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h1>here will shown all about me</h1>
            <Footer></Footer>
        </div>
    );
};

export default About;